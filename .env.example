TARGET_REPO_URL=https://github.com/apache/superset.git
TARGET_REPO_DIR=/workspace/src/superset
OUTPUT_DIR=/workspace/output
SKIP_CLONE=0
ALLOW_FALLBACK_GRAPH=1

# Пример. Уточните актуальные аргументы CLI в документации knowledge-graph.
# KG_COMMAND=knowledge-graph index --path . --format json --output /workspace/output/run-<timestamp>/superset-kag.json
KG_COMMAND=knowledge-graph --help
